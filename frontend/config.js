// Frontend Configuration
// This file can be used to set environment-specific variables
// For production, you can build this file with actual values or use a build tool

window.APP_CONFIG = {
    // API URL - defaults to localhost:3000 if not set
    API_URL: window.API_URL || 'http://localhost:3000/api/todos',
    
    // Other frontend configuration can be added here
    // ENV: 'development' | 'production'
    // DEBUG: true | false
};

